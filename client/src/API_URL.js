// eslint-disable-next-line operator-linebreak
const API_URL =
  window.location.hostname === 'localhost'
    ? 'http://localhost:8000'
    : 'http://api.isitnewyearsday.com';

export default API_URL;
